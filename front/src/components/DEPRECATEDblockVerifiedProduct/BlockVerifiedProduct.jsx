/*import React, { useState } from "react";
import VerifiedIcon from "@mui/icons-material/Verified";
import styles from "../blockVerifiedReservation/BlockVerifiedReservation.module.css";
import { Link } from "react-router-dom";

const BlockVerifiedProduct = () => {
    const [navigateToHomePath, setNavigateToHomePath] = useState(`/`)

    return (
        <div className={styles.backgroundContainer}>
            <div className={styles.verifiedContainer}>
                <VerifiedIcon
                    className={styles.iconVerified}
                    style={{ fontSize: "80px" }}
                />
                <p className={styles.text}>Tu propiedad se ha creado con éxito.</p>
                <div className={styles.btnContainer}>
                    <Link to={navigateToHomePath} className={styles.btn}>
                        Ok
                    </Link>
                </div>
            </div>
        </div>
    );
};

export default BlockVerifiedProduct;*/
